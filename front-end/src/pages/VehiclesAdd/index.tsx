import React from 'react';
// import { v4 } from 'uuid';
// import { useNavigate } from 'react-router-dom';
import {
  // Button,
  Div,
  // Form,
  // Image,
  // Input,
  // InputImage,
  // Label,
  // Options,
  // Prev,
  // P,
  // Section,
  // SelectCategory,
  // Corrucel,
  // Ol,
} from './styled';

function Index() {
  // const imagesListRef = ref(storage, `images/${vehicle.id}`);
  // const usersCollectionRef = collection(db, 'vehicles');
  // const navigate = useNavigate();

  return <Div />;
}

export default Index;
